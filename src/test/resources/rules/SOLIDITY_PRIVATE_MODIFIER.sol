contract MarketPlace {
    // <yes> <report> SOLIDITY_PRIVATE_MODIFIER 5616b2
    uint private data;
    uint data;
    function someComp() {
        if (x.balance < 10 && myAddress.balance >= 10) x.transfer(10);
    }

    function kill() private {

        suicide(msg.sender);
    }
}
