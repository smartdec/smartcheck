pragma solidity ^0.4.5;//5a1037
contract Voting {//5a1547
    function findBidder(bytes32 bidderId) constant returns (uint8 err, uint groupIndex, uint bidderIndex) {//8a1725
      for(bidderIndex = 0; bidderIndex < 34; bidderIndex++) {//5a1633
        }
    return (1, 0, 0);
}
function findBidder(bytes32 bidderId) constant returns (uint8 err, uint groupIndex, uint bidderIndex) {//8a1725
    for(groupIndex = 0; groupIndex < groups.length; groupIndex++) {//8a1600, 5a1633
        for(bidderIndex = 0; bidderIndex < groups[groupIndex].bidders.length; bidderIndex++) {//8a1532, 8a1600, 5a1633
            }
        }
    return (1, 0, 0);
}
function findBidder(uint groupIndex, bytes32 bidderId) constant returns (uint8 err, uint bidderIndex) {//8a1725
    for(bidderIndex = 0; bidderIndex < groups(length); bidderIndex++) {//8a1600, 5a1633
        }
    return (1, 0);
}
function refundLosingGroups(address crowdsaleAddr) {//8a1725

    for (uint groupIndex = 0; groupIndex < groupsCount; groupIndex++) {//8a1601, 5a1633
        for (uint bidderIndex = 0; bidderIndex < biddersCount; bidderIndex++) {//8a1601, 5a1633
        }
        }
    }

}