contract MarketPlace {
    function transfer() {
        uint x = msg.value;
        uint a=x+1;//b91637
        uint b=2*x;//b91637
        a=x-1+8+s(0);//b91637
        a=f(v+1-9);//b91637
        a.send(f+1);//b91637
        a += 10;//b91637
        if (x.balance < 10+a && myAddress.balance >= 10) x.send(10+9);//b91637
        while (3+1<x < 3) {//b91637
            a=x-1+8+s(0);
        }
        for (uint bidderIndex = 0; bidderIndex < biddersCount+8; bidderIndex++) {//b91637
            b=2*x;
        }
    }

}